package com.spectrun.spectrum.repositories;

import com.spectrun.spectrum.models.installModules;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InstallModuleRepository extends JpaRepository<installModules,Long> {
}
